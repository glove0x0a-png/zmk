description: Toggle between two behaviors on each key press

compatible: "zmk,behavior-toggle-action"
include: base.yaml

properties:
  bindings:
    type: phandle-array
    required: true
  label:
    type: string

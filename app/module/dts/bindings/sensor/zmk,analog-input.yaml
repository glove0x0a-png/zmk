description: ZMK Analog Input Sensor

compatible: "zmk,analog_input"

include: [ "adc.yaml" ]

properties:
  io-channels:
    required: true
    type: phandle-array
    description: ADC channel to read
